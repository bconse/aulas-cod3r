// arrays sao organizados por [] e índices
// objetos sao organizados por {} e valores

const numeros = [10, 20, 30]
const produto = {
    nome: 'Caneta',
    preco: 9.99,
    desconto: 0.2
}

//formas de acessas os dados:

// 1ª:
console.log(numeros[0])
console.log(produto['nome'])
console.log(produto['preco'])
console.log(produto['desconto'])

// 2ª:

console.log(produto.nome)
console.log(produto.preco)
console.log(produto.desconto)

